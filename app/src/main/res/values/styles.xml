<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="bottomIndicatorColorTint">@color/color_indicator_bottom</item>
        <item name="textColorSecondary">@color/gray</item>
        <item name="textColorPrimary">@color/black</item>
        <item name="textColorTitle">@color/black</item>
        <item name="textColorAlert">@color/colorPrimary</item>
        <item name="cardBackground">@color/white</item>
        <item name="contentBackground">@color/whitesmoke</item>
        <item name="tabIndicatorColor">@color/white</item>
        <item name="imageBackgroundTint">#00000000</item>
        <item name="android:windowIsTranslucent">false</item><!--X5设置防止视频上屏透明-->
    </style>

</resources>
